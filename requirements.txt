# ConCor-1 requirements.
#
# Install torch/torchvision first if you need a specific CUDA build, e.g.
#   pip install torch==2.8.0 torchvision==0.23.0 --index-url https://download.pytorch.org/whl/cu128
# The released checkpoint was trained and evaluated with torch 2.8.0 / cu128.

torch>=2.8.0
torchvision>=0.23.0          
transformers==5.3.0

safetensors>=0.4.0
huggingface_hub>=0.30.0
numpy>=1.24
pillow>=10.0

# Qwen3.5's gated-delta-net (linear-attention) kernels. Pure Python + Triton,
# so this installs without a compiler.
flash-linear-attention>=0.4.1

# --- Optional -----------------------------------------------------------------
# Both need a CUDA compiler
#
#   flash-attn>=2.8.0      (otherwise pass --attn_implementation sdpa)
#   causal-conv1d>=1.4.0   fused depthwise causal conv for the linear-attention
#                          layers; transformers falls back to pure torch without it
